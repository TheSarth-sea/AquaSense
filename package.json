{
  "name": "aquasense-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">= 22.0.0"
  },
  "scripts": {
    "install": "npm install --prefix backend",
    "build": "echo \"Nothing to build\"",
    "start": "npm start --prefix backend"
  }
}
